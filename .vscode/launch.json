{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "yarn workspace site run dev",
      "type": "pwa-node",
      "request": "launch",
      "program": "${workspaceFolder}/site/node_modules/.bin/gatsby",
      "cwd": "${workspaceFolder}/site",
      "args": ["develop"],
      "runtimeArgs": ["--nolazy"],
      "console": "integratedTerminal"
    }
  ]
}
